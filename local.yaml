# YAML configuration file to set the databse location and so
{
      'ismrdb_path': '/data/storage/trop/users/plas/SW',
      # 'ismrdb_path': '/Users/plas/data/SW'
      # 'ismrdb_name': 'test_scint_new_{}.db',
      # 'ismrdb_name': 'scint_new_{}.db',
      # 'ismrdb_name': 'scint_reduced_{}.db',
      'ismrdb_name': 'scint_reduced_{}_CAL_test.db',
      'tabname': 'sep_data_{}'
}
